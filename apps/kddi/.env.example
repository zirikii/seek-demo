# KDDI NOC Dashboard — environment example
# Copy to `.env` to override defaults. All values are safe demo defaults (no secrets).

# Product name shown in the header / document title
VITE_APP_NAME=KDDI Network Operations

# Live-feed simulation interval in milliseconds (default 45s)
VITE_AUTO_REFRESH_INTERVAL_MS=45000
